/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Guardianes-Antillama',
  assetPrefix: '/Guardianes-Antillama/',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
